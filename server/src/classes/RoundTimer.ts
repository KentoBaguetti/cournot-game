// this will be made later, currently timer logic is handled by the games directly, but moving them into a class is better longe term
export class RoundTimer {
  //private TimerInterval: NodeJS.Timeout;

  constructor() {}
}
